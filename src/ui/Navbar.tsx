import MobileNavBar from "./MobileNavBar";
import DesktopNavBar from "./DesktopNavBar";

const Navbar = () => {
  return (
    <>
      <MobileNavBar />
      <DesktopNavBar />
    </>
  );
};

export default Navbar;
